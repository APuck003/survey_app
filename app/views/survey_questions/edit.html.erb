<div class="row">
  <div class="col s12 m6">
    <h4>Edit Survey Question</h4>

    <%#= form_with model: @survey do |f| %>
      <%#= f.fields_for :question do |questions_form| %>
    <%= form_for @question do |questions_form| %>
      <%#= f.fields_for @question do |questions_form| %>
        <p>
          <%= questions_form.check_box :_destroy %>
          <%= questions_form.label :question %>
          <%= questions_form.text_field :question %>
        </p>

        <p>
          <%= questions_form.submit "Edit" %>
        </p>
      <%# end %>
    <% end %>

  </div>
</div>
